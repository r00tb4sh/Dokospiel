{
  "name": "dokospiel",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --platform=browser --minify && cp index.html index.css dist/"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}